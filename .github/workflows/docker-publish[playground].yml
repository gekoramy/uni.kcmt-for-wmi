name: Docker[playground]

on:
  push:
    branches: [ "main" ]
    paths: [ "image/playground/**" ]
  workflow_dispatch:

jobs:
  call-workflow:
    uses: ./.github/workflows/docker-publish.yml
    permissions:
      contents: read
      packages: write
    with:
      folder: playground
